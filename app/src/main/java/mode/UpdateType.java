package mode;

public enum UpdateType {
    USERNAME,
    PRENAME,
    SURNAME,
    BIRTHDATE,
    E_MAIL_ADDRESS,

    STREET,
    HOUSE_NUMBER,
    POST_CODE,
    CITY,
    COUNTRY,

    NAME,
    ACTIVITY,
    MAX_PARTICIPANTS,
    ACTUALLY_PARTICIPANTS,
    FINISHED_STATUS
}
